name: JSaction
on: push
jobs:
  testing-actiobn:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: ./.github/actions/hello-docker1
        id: hello
        with:
          who-to-greet: "MEWorld"
      - run: |
          echo "Time:${{ steps.hello.outputs.time }}"
